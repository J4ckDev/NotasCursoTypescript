# Namespaces e importaciones

Los namespaces permiten tener más ordenado el código al permitir la agrupación de funciones. Un ejemplo de uso es el archivo [validations.ts](./validations.ts).

Ahora en la actualidad lo que más se usa son las exportaciones e importaciones, por lo que para er un ejemplo sobre esto puede ejecutarse el proyecto de la carpeta [ProyectoInicial](./ProyectoInicial).
